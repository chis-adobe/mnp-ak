auto-generated: true
indices:
  all:
    include:
      - /**
    target: /query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
  offices:
    include:
      - /offices/**
    target: /offices/query-index.json
    properties:
      city:
        select: head > meta[name="city"]
        value: |
          attribute(el, "content")
      address:
        select: head > meta[name="address"]
        value: |
          attribute(el, "content")
      province:
        select: head > meta[name="province"]
        value: |
          attribute(el, "content")
      postalCode:
        select: head > meta[name="postal-code"]
        value: |
          attribute(el, "content")
      phone:
        select: head > meta[name="phone"]
        value: |
          attribute(el, "content")
      fax:
        select: head > meta[name="fax"]
        value: |
          attribute(el, "content")
      addressFragment:
        select: head > meta[name="address-fragment"]
        value: |
          attribute(el, "content")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
  addresses:
    include:
      - /fragments/addresses/**
    target: /fragments/addresses/query-index.json
    properties:
      city:
        select: main .address > div:nth-child(1) > div:nth-child(2)
        value: |
          textContent(el)
      address:
        select: main .address > div:nth-child(2) > div:nth-child(2)
        value: |
          textContent(el)
      province:
        select: main .address > div:nth-child(3) > div:nth-child(2)
        value: |
          textContent(el)
      postalCode:
        select: main .address > div:nth-child(4) > div:nth-child(2)
        value: |
          textContent(el)
      phone:
        select: main .address > div:nth-child(5) > div:nth-child(2)
        value: |
          textContent(el)
      fax:
        select: main .address > div:nth-child(6) > div:nth-child(2)
        value: |
          textContent(el)
  blog:
    include:
      - /blog/**
    target: /blog/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      date:
        select: head > meta[name="publication-date"]
        value: |
          dateValue(attribute(el, 'content'), 'MM-DD-YYYY')
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
      featured:
        select: head > meta[name="featured"]
        value: |
          attribute(el, "content")
      author:
        select: head > meta[name="author"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      content:
        select: main > div
        values: characters(textContent(el), 0, 200)
      tags:
        select: head > meta[property="article:tag"]
        values: |
          attribute(el, 'content')
  articles:
    include:
      - /insights/directory/**
    target: /insights/directory/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      date:
        select: head > meta[name="publication-date"]
        value: |
          attribute(el, "content")
      synopsis:
        select: head > meta[name="synopsis"]
        value: |
          attribute(el, "content")
      insights:
        select: head > meta[name="insights"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[name="image"]
        value: |
          attribute(el, "content")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
  authors:
    include:
      - /author/**
    target: /author/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
