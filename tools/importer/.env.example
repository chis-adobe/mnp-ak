# Copy this file to `.env` (same folder) and paste your values.
# `.env` is gitignored — it will never be committed.

# IMS bearer token from a signed-in da.live session (required to --publish).
DA_TOKEN=

# Optional: override the Supabase source (defaults are baked into supabase-source.js).
# SUPABASE_URL=https://wdgjvnbgtulfuevdtvjk.supabase.co/rest/v1
# SUPABASE_KEY=sb_publishable_...
